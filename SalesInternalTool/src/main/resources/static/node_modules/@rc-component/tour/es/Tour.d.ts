import type { ReactNode } from 'react';
import type { Gap } from '@rc-component/tour/es/hooks/useTarget';
import type { TourStepInfo } from '@rc-component/tour/es/TourStep';
import type { TourStepProps } from '@rc-component/tour/es/TourStep';
import type { PlacementType } from '@rc-component/tour/es/placements';
export interface TourProps {
    steps?: TourStepInfo[];
    open?: boolean;
    defaultCurrent?: number;
    current?: number;
    onChange?: (current: number) => void;
    onClose?: (current: number) => void;
    onFinish?: () => void;
    mask?: boolean;
    arrow?: boolean | {
        pointAtCenter: boolean;
    };
    rootClassName?: string;
    placement?: PlacementType;
    prefixCls?: string;
    renderPanel?: (props: TourStepProps, current: number) => ReactNode;
    gap?: Gap;
    animated?: boolean | {
        placeholder: boolean;
    };
}
declare const Tour: (props: TourProps) => JSX.Element;
export default Tour;
